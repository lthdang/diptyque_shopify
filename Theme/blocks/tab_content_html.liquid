{% if block.settings.code != blank %}
  <div class="tab-content-html" {{ block.shopify_attributes }}>
    {{ block.settings.code }}
  </div>
{% endif %}

{% schema %}
{
  "name": "Custom HTML",
  "tag": null,
  "settings": [
    {
      "type": "liquid",
      "id": "code",
      "label": "HTML / Liquid"
    }
  ]
}
{% endschema %}
